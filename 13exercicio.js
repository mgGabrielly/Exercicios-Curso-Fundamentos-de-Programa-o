function filtrarnumeros(array){
    var filtro = new Array;
    var array1 = Array(array);
    let tam = length.array;
    for(let i = 0; i < tam; i++){
       if (typeof(array1) == Number){
        filtro;
        } 
    }
}